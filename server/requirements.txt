# התלויות היחידות מעבר לספריית התקן של פייתון — ומוצמדות, לא ">=".
#
# ההצמדה היא לגרסה שדביאן 13 (trixie) מספקת, כי זו הגרסה שכל שרת ImageCtl
# מריץ בפועל: install/setup-boot-server.sh מתקין python3-fastapi
# ו-python3-uvicorn ב-apt, לא ב-pip. כלומר הצמדה לגרסה הזו אינה "לבדוק
# ספרייה ישנה" — היא לבדוק את הספרייה שנפרסת.
#
#   apt-get install python3-fastapi python3-uvicorn
#   # דביאן 13: 0.115.11-4 ו-0.32.0-3
#
# ‏`fastapi>=0.110` הלא-מוצמד היה השורש של #113: המעבדה פתרה 0.115.11
# וה-CI פתר 0.141.1, ובין השתיים include_router הפסיק לשטח ניתובים אל
# app.routes. הטסט שסינן לפי r.path קיבל רשימה ריקה, "עבר" בלי לבדוק דבר
# בסביבה אחת ונכשל בשנייה, וה-CI היה אדום 12 קומיטים בזמן שהמעבדה הייתה
# ירוקה. כל עוד זה לא מוצמד, "עבר במעבדה" אינו אומר "יעבור ב-CI".
#
# שדרוג הוא החלטה, לא תוצאה של תאריך ההתקנה: מרימים את המספר כאן ביחד עם
# חבילת ה-apt שהשרת יקבל, ומריצים את החבילה על שתי גרסאות הפייתון שב-CI.
fastapi==0.115.11
uvicorn==0.32.0
