# Handoff — ‏2026-09-02 (יום מלא: השרשרת שאושרה + הצי החינמי)

> המסירה הקודמת: [`handoff-2026-08-31-night.md`](handoff-2026-08-31-night.md).
> הפירוט המבצעי המלא: שתי רשומות היומן ב-[`logs/2026-09-02.md`](../logs/2026-09-02.md).

## בוצע

**בוקר — התוכנית שאושרה, כולה מוזגה ירוק:**
- ‏`doctor` (#270) — גלאי ל-workflow שלא רץ בכלל; תפס את log-review (0 ריצות), שהופעל ועבר.
- מכסה-לפני-הוצאה (#271) — ‏`Request-AgentSpend` ב-quota-lib: רישום לפני קריאה, אזהרה ב-80%.
- שער-לפני-PR (#273) — ‏`gate-pr.sh` + פסק-דין שלישי `insufficient_evidence` + אכיפת שרת (`pr-gate.yml`) על ענפי `auto/*`.
- מניפסט מיוצר (#274) — ‏`gen-manifest.py`; ‏drift ב-`in_use_by` מאדים CI.
- דילוג מוצהר ב-`otogit-sync` (#268) ובסקירת otogit — דפוס הצהרת-כוונה (var+secret): אין הצהרה=דילוג גלוי, יש הצהרה בלי סוד=אדום. **נמדד בייצור.**
- ‏otogit: ‏quota-check עם 4 מצבים (‏ok/broken/expired/absent) + הכלל "כלי בלי טסט לא נכנס" ב-START.md.

**ערב — הצי החינמי (#276, ‏7 PRs):**
- ‏LiteLLM proxy על `127.0.0.1:4000` — ‏13 פריסות מ-7 ספקים (‏Groq, NVIDIA NIM, Mistral, Gemini×2 מפתחות, Cloudflare, OpenRouter), **כולן ענו בקריאה אמיתית נמדדת**. קבוצות לפי אופי: ‏big-context / bulk-short / code / general.
- ‏`fleet_budget.py` — מונה בתוך ה-proxy: ספירה לפי מודל+מפתח+חלון, חסימה לפני יציאה במיצוי, חלון (יומי/שבועי/חודשי) מהמניפסט.
- ‏`model-watch.py` — מודל שנעלם מקטלוג הספק נתפס. ‏`limits-watch.py` — עוגן בדף המכסות הציבורי של כל ספק; עוגן שנעלם = התראה.
- ‏`decision-ack.yml` (#284) — תגובת נדב ב-#212/#215 מקבלת "נקלט" ונפתחת כ-Issue עם `agent:ready`.
- מענה מלא לארבע תשובות נדב שנזנחו: ‏#244 נמדד (‏GitHub מקבל `refs/claims`, ‏CAS עובד) · שורש הקודקס נמצא (‏sandbox חוסם `%LOCALAPPDATA%`) · ‏#240 נבדק ברשת (המקובל: תג לכל release).

## הוחלט

- **‏hard $0 הוא מבני, לא נוהג** — רק ספקים חינמיים בקונפיג, ‏fallback פנימה בלבד, ‏openrouter רק `:free`, מפתחות כהפניות סביבה. הכול בטסט `test_free_fleet.py`.
- ‏Cerebras **הוסר** (צילום Billing של נדב: אפס מענקים) · ‏Tessera **לא חובר** (הוא proxy, לא ספק).
- אימון ספקים על הדאטה **מקובל על נדב** ("דיל לגיטימי") — הגבול: סודות לא עוברים בפרומפטים.
- קודקס לא יעשה גיט — הרץ יתבע ויפתח PR בשבילו.
- כשל של מונה התקציב מרעיש ולא משבית (החרגה מודעת מ"נכשל סגור", מתועדת ב-`fleet_budget.py`).
- לחינמיים: Issues/תיוג/קומיטים/CHANGELOG/סריקות/תיעוד. על המנוי: הכרעות main, ניפוי, שרשרת/חוזים. **תוצר חינמי לא נכנס ל-main בלי בדיקת המתאם** (ב-AGENTS.md).

## קבצים

`tools/agents/{litellm.yaml, free-fleet.ps1, fleet_budget.py, model-watch.py, limits-watch.py, doctor.py, gen-manifest.py, gate-pr.sh, gate_verdict.py, quota-lib.ps1, providers.json}` · `.github/workflows/{pr-gate, decision-ack, otogit-sync, shellcheck}.yml` · `AGENTS.md` · `.env.example` · טסטים חדשים ×10 · ‏otogit: `tools/quota-check.py`, ‏START.md.

## נשאר

1. **דוח החיסכון לנדב — ‏2026-09-04** (הובטח; המונים כבר סופרים).
2. חיבור הסוכנים בפועל ל-`:4000` — המנגנון קיים, אף סוכן עוד לא צורך דרכו משימה אמיתית.
3. הכרעות נדב: ‏#240 ("ב' מאושר"?) · ‏PATs לשני המשתמשים שפתח (‏okednadav, nadavok) · סודות otogit (‏GEMINI_API_KEY+REVIEW_ENABLED) ו-OTOGIT_TOKEN+OTOGIT_SYNC_ENABLED.
4. ‏#272 — פלייקי `database is locked` ב-test_concurrency (‏CI, ריצה חוזרת עברה).
5. ‏84 התראות CodeQL פתוחות (3 high) — לא טופלו מאז 29/08.
6. הטלפון של נדב לא מקבל דחיפות גם כשנשלחו בפועל — הבעיה בצד הטלפון, לא נסגר.

## שים לב

- **ה-proxy רץ ידנית** ברקע של הסשן — אחרי ריסטארט צריך `free-fleet.ps1` מחדש. ‏`-Check` מריץ גם model-watch.
- ‏`.env` ב-`C:\ImageCtl` הוא של נדב — לא לקרוא ערכים, לא להדפיס, לא להעלות. ‏`.env.example` הוא התבנית.
- עץ העבודה ב-`C:\ImageCtl` מכיל שינויים לא-קמוטים של סשן אחר (‏server/agentctl וכו') — **לא לגעת**; לעבוד ב-worktrees.
- מוקשים שנמדדו היום: ‏litellm קורא קונפיג ב-cp1252 בווינדוס (`PYTHONUTF8=1` בסקריפט) · ‏ps1 עברי חייב BOM · ‏Groq מחזיר 403 ל-UA של urllib · ‏kwargs של callback מחזיר מודל בלי קידומת ספק · ‏NIM איטי בתור החינמי (4–5 דק', ‏timeout 420s) — לא שבור.
- הזיכרון המתמשך עדכני: ‏`free-fleet-litellm.md` ב-memory.
