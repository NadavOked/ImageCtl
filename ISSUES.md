<div dir="rtl">

# כלל עבודה מחייב לגבי Issues

המסמך הזה הוא תהליך קשיח לסוכן. הוא נולד מהכללים שכבר כתובים ב-`CONTRIBUTING.md`
ובתבניות `.github/ISSUE_TEMPLATE/`, אחרי שממצאים נפתחו "מהר" ונשכחו בלי
label, milestone, או מקום בלוח.

**Issue לא נחשב שנוצר עד שכל ה-metadata שלו מלא.**
יצירה בלי label / milestone / project / dependency רלוונטיים היא פעולה
לא גמורה. יש להשלים אותם מיד לאחר היצירה ולבדוק אותם מחדש.

אין לתקן ממצא צדדי תוך כדי העבודה רק כדי להימנע מפתיחת Issue. אם הוא מחוץ
ל-scope, פותחים Issue מלא וממשיכים במשימה המקורית.

## הזרימה

חוקר → מוצא ממצא → בודק שאין כפילות → מוכיח אותו → פותח Issue מלא → ממלא
metadata וקשרים → רק אז ממשיך לעבוד.

ולא: חוקר → "מצאתי עוד באג" → כותב לעצמו הערה → ממשיך ושוכח ממנו.

## לפני יצירת Issue

1. לבדוק אם כבר קיים Issue שמתאר את אותה בעיה או אותה דרישה. אין ליצור כפילות.
2. לא לפתוח Issue על חשד בלבד. Issue ייפתח רק כאשר יש ראיה מספקת: קוד, טסט, לוג, הרצה, CI, או מדידה.
3. לסווג את הממצא כ-Bug / Enhancement / Task בהתאם למהות שלו.
4. להשתמש ב-Issue template הקיים בריפו. אין ליצור מבנה מקוצר.
5. למלא את כל השדות הרלוונטיים, ולא להשאיר סעיפים ריקים.
6. ב-Bug חובה לתעד:
   * רכיב
   * תיאור
   * שורש הבעיה המדויק
   * מה נשבר בפועל
   * מה אסור לעשות בתיקון
   * הגדרת "גמור"
   * איך הממצא נמצא
   * מה עדיין לא אומת
7. ב-Enhancement חובה לתעד:
   * רכיב
   * מה רוצים
   * למה זה חשוב
   * מה קורה היום
   * החלטות שכבר נעולות
   * מה אסור לעשות
   * הגדרת "גמור"
   * מקור הדרישה
   * מה עדיין לא אומת
8. להוסיף ל-Issue את כל ה-metadata הרלוונטי:
   * Label מתאים — **סוג** (`bug` / `enhancement` / `task`) **ורכיב**
     (`server` / `agent` / `boot` / `console` / `installer` / `lab` / `ci` / `tests` /
     `documentation`). ‏Issue Types של GitHub אינם זמינים כאן — הריפו הוא של
     חשבון משתמש ולא ארגון (`repos/.../issues/types` מחזיר 404), ולגרסת ה-`gh`
     המותקנת אין דגל `--type`. **הסוג נקבע בתווית.**
   * Milestone מתאים
   * Project (לוח העבודה)
   * Assignee אם יש אדם שאחראי עליו
9. אם Issue תלוי ב-Issue אחר, ליצור dependency אמיתי של GitHub (`blocked by` / `blocking`) ולא להסתפק בכתיבת "תלוי ב-#123" בתוך הטקסט.
10. אם מדובר בתת-משימה של Issue קיים, להשתמש ב-Sub-issue ולא רק בקישור טקסטואלי.
11. אם הממצא הוא חולשת אבטחה, לא לפתוח Issue ציבורי. להשתמש ב-Security Advisory. זה כבר מוגדר בתבנית.
12. לאחר יצירת Issue, לוודא בפועל שכל ה-metadata הוגדר ולא להסתפק בכך שהפקודה ליצירה הצליחה.
13. לתת ל-Issue מספר ולשמור אותו בהקשר של העבודה הנוכחית.
14. אם הממצא משפיע על העבודה הנוכחית, לקשר אותו ל-PR/Issue הרלוונטי.

במהלך כל חקירה, סקירת קוד, בדיקה, הרצה או מימוש — אם נמצא באג, פער, סיכון, משימה או יכולת חדשה שאינה חלק מהמשימה הנוכחית, אין להתעלם ממנו.

## פקודות

**הדרך המומלצת היא `tools/issue-new.sh`** — הוא מריץ את כל הרביעייה
שלמטה כיחידה אחת, **מסרב לרוץ** כשחסר title / label / milestone (עוד
לפני שנגע ב-`gh`), וקורא את השדות בחזרה. רביעייה שמועתקת ביד נחתכת
אחרי הפקודה הראשונה, וזו שנחתכה מחזירה URL ונראית כמו הצלחה — כך
נוצרו 70 ה-Issues בלי milestone.

</div>
```bash
tools/issue-new.sh --title "..." --label server --label bug \
    --milestone "חוסמי פריסה במכללה" --body-file body.md [--blocked-by "#123"]
```

<div dir="rtl">

הפקודות שהוא מריץ, למי שצריך אותן בנפרד:

</div>
```bash
# פתיחה — הכל בפקודה אחת
gh issue create --title "..." --label server,bug --milestone "חוסמי פריסה במכללה"

# הלוח (ImageCtl — לוח עבודה)
gh project item-add 2 --owner NadavOked --url <URL>

# תלות
gh api --method POST repos/NadavOked/ImageCtl/issues/<N>/dependencies/blocked_by -F issue_id=<id>

# אימות — קוראים את השדות בחזרה. שההרצה הצליחה אינו ראיה שהם נקבעו.
gh issue view <N> --json labels,milestone,projectItems
```

<div dir="rtl">


גוף ה-Issue הוא ראיה, לא תיאור: מספרי שורות, פלט אמיתי, ומדידה. "נראה שיש בעיה" אינו Issue.

</div>
