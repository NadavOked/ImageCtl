<div dir="rtl">

# תהליך העבודה

המסמך הזה מתעד את התהליך **כפי שהוא נאכף בפועל**, לא כפי שהיינו רוצים
שיהיה. הוא נכתב אחרי שהתהליך התגבש בעבודה, וכל כלל בו נולד מכשל אמיתי.

**הפרויקט אינו מחפש תורמים חיצוניים.** הוא משרת מכללה אחת, והוא ציבורי
כדי שיהיה ניתן לבדיקה — לא כהזמנה. המסמך הזה קיים כדי שמי שכן יעבוד
עליו — נדב, מפתח אחר, או סוכן AI — לא יגלה את הכללים בדרך הקשה.

**קרא קודם [`CLAUDE.md`](CLAUDE.md).** הוא מכיל את העקרונות שאסור לשבור,
את ה-gotchas שנלמדו בדם, ואת פקודות הבנייה. המסמך הזה הוא **התהליך**;
הוא **התוכן**.

---

## שלושה כללים שאינם ניתנים למשא ומתן

### 1. ענף → PR → CI ירוק → מיזוג

**לא דוחפים ישירות ל-`main`.** לכל תיקון ענף משלו, PR, ובגוף ה-PR
`closes #N`.

> ⚠️ **`closes #N` בתוך backticks לא עובד.** ‏GitHub אינו מפרש מילות
> סגירה בתוך code span, וה-Issue יישאר פתוח. קרה, ונסגר ידנית.

### 2. כל commit מסתיים ✓ ירוק

בודקים `gh run list` **אחרי כל push** — כולל אחרי המיזוג עצמו, לא רק
על ה-PR. ‏CI היה אדום 12 קומיטים ואיש לא הבחין (#113).

**‏PR ירוק אינו main ירוק, וזה לא ניסוח מחמיר — זה שני דברים שונים.**
ה-PR נבדק על מיזוג *מדומה* של הענף עם main כפי שהיה **באותו רגע**;
הריצה שאחרי המיזוג היא הראשונה שרצה על הקוד המשולב בפועל.

ב-2026-08-31 זה עלה במחיר: ‏`02f2d67` (מיזוג #159) נמזג אחרי 8 בדיקות
ירוקות, והריצה שאחריו **על main** נכשלה. איש לא הסתכל. מה שהיא הסתירה
היה [#177](https://github.com/NadavOked/ImageCtl/issues/177) — ‏`room.tick`
בתוך מסלול ה-hello עושה `close` ואז `open` בלי אטומיות, שני hello
במקביל מתנגשים על סלוט המולטיקאסט, ו-**hello מחזיר 500 בזמן שהגל הבא
אינו נפתח**. באג שהוא בדיוק מה שחדר השיכפולים קיים בשבילו.

**וכשקומיט יוצא אדום על ענף שאיש עוד לא סקר — ‏`git commit --amend`
ו-`push --force-with-lease`, לא קומיט תיקון נוסף.** אחרת main מקבל
קומיט שבור לתמיד ו-`git bisect` נתקע עליו. שלושה כאלה נכנסו באותו
לילה (`6721a88`, `39a9898`) לפני שזה נתפס. **היסטוריה שכבר ב-main לא
משוכתבת** — ‏`push --force` על main גרוע יותר, באותו היגיון שבו הפרויקט
הכריע לא למחוק תג שפורסם.

### 3. טענת אימות נוקבת בסביבה

**"עובר במעבדה" אינו "עובר ב-CI".** שתי הסביבות נבדלות בגרסאות ספריות
ובכרטיסי רשת. אומרים **היכן** נבדק, תמיד.

---

## בדיקות

**הטסטים רצים על מעבדת ה-VM, לא על תחנת הפיתוח.** בווינדוס `test_fanout`
מדולג ו-`WinError 50` מפיל את הריצה הרב-קבצית (#14).

</div>

```bash
TMPDIR=/root/<שלך>-tmp python3 -m pytest tests -q
```

<div dir="rtl">

**עץ העבודה נוצר בפקודה אחת שמאמתת את עצמה:**
`tools/git/lab-work-checkout.sh <מספר-Issue>` — הוא מוודא שה-HEAD
שהתקבל זהה ל-`origin/main` שנמשך זה עתה, ונכשל בקול אם לא. ‏`git clone`
רגיל מהמעבדה **יכול לרשת `main` מיושן ולהצליח בשקט** (#190).
‏`tools/git/README.md` מסביר, עם המדידה.

**`TMPDIR` משלך הוא חובה** — ‏`/tmp` על המעבדה הוא tmpfs של 2GB שהפיל
כבר שלוש ריצות.

**ריצה אחת ירוקה אינה ראיה.** מריצים **פעמיים לפחות**; טסט תלוי-תזמון —
**עשר פעמים**. מנהל מסר "827 עוברים" על טסט שנכשל ב-4 מתוך 5 ריצות.

---

## בקרה שלילית — הכלל שנאכף הכי חזק

**טסט שלא ראית נכשל אינו טסט.** לכל תיקון:

1. כותבים את הטסט
2. **מחזירים את הקוד למצב שלפני התיקון** (`git stash`)
3. **מריצים ומראים את פלט הכישלון**
4. מחזירים, ומריצים שוב

**הכישלון חייב להיות על התנהגות.** ‏`ImportError` או קובץ חסר **אינם
בקרה שלילית** — הם מוכיחים שהטסט לא רץ, לא שהוא בודק משהו. זה קרה
פעמיים, ובשתיהן הטסט היה חסר ערך.

**וכדאי לספור גם את מה שעובר בשני המצבים** — אלה השומרים מפני תיקון-יתר.

---

## Issues

**כל Issue מקבל `label`, ‏`milestone` ומקום בלוח בפתיחה** — בפקודה
עצמה, לא בסבב ניקיון אחר כך.

**‏Issue אינו נחשב שנוצר עד שה-metadata שלו מלא.** יצירה בלי label,
‏milestone, מקום בלוח וקשרי תלות רלוונטיים היא **פעולה לא גמורה** — לא
"כמעט". ‏`gh issue create` שהחזיר URL אינו ראיה שהשדות נקבעו: קוראים
אותם בחזרה. זה עיקרון 5 בדיוק כפי שהוא.

הכלל הזה נכתב מוקדם **ולא קוים**: ב-2026-08-31 נמצאו **70 Issues
סגורים בלי milestone** ושניים בלי label. הפער לא נוצר מהחלטה אלא
מכך שכל Issue בודד נפתח "מהר" ואיש לא חזר. תיקון רטרואקטיבי גם
מסוכן: הוא מפתה לשייך עבודה גמורה ליעדי אימות **עתידיים** ולנפח
אותם, וזה בדיוק מה שקרה שם ל-5 Issues לפני שנתפס.

**גוף ה-Issue הוא ראיה, לא תיאור:** מספרי שורות, פלט אמיתי, ומדידה.
"נראה שיש בעיה" אינו Issue.

**תלויות נרשמות כקשרים** (`blocked by` / `blocking`) ולא כטקסט — אחרת
אי-אפשר לשאול "מה חוסם את X". תת-משימה נרשמת כ-Sub-issue, לא כקישור.

**ממצא צדדי אינו מתוקן "על הדרך" ואינו נזנח.** במהלך חקירה, סקירה,
בדיקה או מימוש — באג, פער, סיכון או יכולת שאינם חלק מהמשימה הנוכחית
מקבלים Issue מלא לפי התבנית, וממשיכים במשימה המקורית. תיקון מחוץ
ל-scope כדי להימנע מפתיחת Issue הוא בדיוק הדפוס שיצר את הפער למעלה.

**חולשת אבטחה אינה Issue ציבורי.** הריפו ציבורי, ו-Issue מפרסם את
החולשה לפני שיש תיקון. הדיווח דרך Security Advisories, כפי שתבנית
ה-Issue כבר אומרת.

**ממצא שאינו מטופל בְּ-PR הופך ל-Issue לפני המיזוג.** הערה של סוקר —
אדם או בוט — חיה בתוך ה-PR, ו**נקברת ברגע שהוא מתמזג**. התרעת
`code-scanning` דווקא נשארת עד שמתקנים או דוחים בנימוק, אבל גם היא
נראית רק למי שפותח את הלשונית: ב-2026-08-31 ישבו שם **שתי התרעות
`high`** שעות, ואחת מהן — הסיסמה שנכתבת ליומן systemd — הפכה
ל-[#179](https://github.com/NadavOked/ImageCtl/issues/179) רק אחרי
ששאלו עליהן.

‏CodeRabbit **אינו** יודע לפתוח Issue (נבדק ברשימת הפקודות שלו), ולכן
זה נופל על מי שממזג. **מה שלא הפך לפריט מעקב — לא קרה.**

**‏Issue Types של GitHub אינם זמינים בריפו הזה** — הוא של חשבון משתמש
ולא של ארגון (`repos/NadavOked/ImageCtl/issues/types` מחזיר 404), ולגרסת
ה-`gh` המותקנת אין דגל `--type`. **הסוג נקבע בתווית** — `bug`,
‏`enhancement` או `task`.

התהליך המלא לסוכן — הזרימה, מה חייב להופיע בגוף כל סוג Issue, ומה
בודקים לפני הפתיחה — מפורט ב-[`ISSUES.md`](ISSUES.md).

</div>

```bash
# פתיחה — הכל בפקודה אחת
gh issue create --title "..." --label server,bug --milestone "חוסמי פריסה במכללה"

# הלוח (ImageCtl — לוח עבודה)
gh project item-add 2 --owner NadavOked --url <URL>

# תלות
gh api --method POST repos/NadavOked/ImageCtl/issues/<N>/dependencies/blocked_by -F issue_id=<id>

# אימות — קוראים בחזרה, לא סומכים על שההרצה הצליחה
gh issue view <N> --json labels,milestone,projectItems
```

<div dir="rtl">

---

## גרסאות

**`minor` רק ליכולת שהמפעיל רואה** — מסך חדש, כפתור חדש, משהו שאפשר
להצביע עליו. **כל השאר `patch`**, כולל מודול פנימי חדש, כלי מעבדה
ו-refactor. **תג לכל תיקון.**

---

## הכלל שמעל כולם

`CLAUDE.md`, עיקרון 5:

> **פעולה שלא הצליחה לבדוק — נכשלת, לא מוותרת.** "לא הצלחנו לבדוק"
> ו"בדקנו, הכל תקין" הם שני מצבים שונים, ואסור לקפל אותם לאחד.

לפני כל `|| true`, ‏`2>/dev/null`, ‏`SilentlyContinue` ו-`skipif` —
לשאול מה קורה כשהבדיקה **עצמה** נכשלת. **אם התשובה היא "נראה כמו
הצלחה", זה באג.**

הדפוס הזה חזר בכל שכבה בפרויקט. הרשימה ב-`CLAUDE.md` מונה שבע פעמים.

</div>
