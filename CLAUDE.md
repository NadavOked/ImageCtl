# ImageCtl — הקשר לעוזרי AI

שרת אימג'ים ופריסה למכללה. מחליף שיכפול SSD ידני בשידור מולטיקאסט.
הבעלים: נדב (NadavOked, מגיע מ-IT, לא כותב קוד בעצמו — להסביר החלטות).

> ## 📍 קרא את שלושת אלה לפני שאתה נוגע בקוד
>
> 1. **[`docs/handoff-2026-09-02.md`](docs/handoff-2026-09-02.md)** — המסירה
>    האחרונה: מה מצב המעבדה, מה נעול, מה הבא בתור, ומה ממתין להכרעת נדב.
>    **המסירות משורשרות** — כל אחת מפנה לקודמת.
> 2. **[`CONTRIBUTING.md`](CONTRIBUTING.md)** — התהליך שנאכף בפועל: ענף→PR→CI
>    ירוק→מיזוג, בקרה שלילית מוכחת, ואיפה הטסטים רצים.
> 3. **[`ISSUES.md`](ISSUES.md)** — התהליך המחייב לפתיחת Issue. **‏Issue
>    אינו נחשב שנוצר עד שה-metadata שלו מלא**, וממצא צדדי אינו מתוקן
>    "על הדרך" ואינו נזנח — הוא מקבל Issue משלו.
>
> הקובץ הזה הוא **מה שאסור לשבור**. המסירה היא **איפה הדברים עומדים**.
> ‏`CONTRIBUTING.md` ו-`ISSUES.md` הם **איך עובדים**. כולם נדרשים, ואף
> אחד מהם אינו מספיק לבדו.

**סוכן שלוקח עבודה בעצמו** קורא גם את [`AGENTS.md`](AGENTS.md) — החוזה:
הקצאה עצמית, גבולות קבצים, ייחוס, יומן משימות וחוזה הפלט. הוא **אינו**
משוכפל כאן בכוונה: ‏Grok קורא את שני הקבצים, ותוכן כפול היה מגיע אליו
פעמיים. שני הנהלים החוזרים ביותר נשלפים כסקילים —
[`bakara-shlilit`](.claude/skills/bakara-shlilit/SKILL.md) (בקרה שלילית)
ו-[`hariza-bemaabada`](.claude/skills/hariza-bemaabada/SKILL.md) (הרצה על
מעבדת ה-VM). עותק זהה בית-בבית יושב ב-`.agents/skills/` בשביל Codex,
‏Grok ו-Gemini, ו-`tests/test_agent_skills.py` נופל אם השניים נסחפו.

## מבנה ופקודות

| | |
|---|---|
| Stack | Python **3.12 ו-3.13** — דביאן 13 (הייצור וגם מעבדת ה-VM) מריצה 3.13, תחנת הפיתוח 3.12, וה-CI מריץ את שתיהן במטריצה. FastAPI/uvicorn בשרת בלבד, **מוצמדים** ב-`server/requirements.txt` לגרסת דביאן 13 (‏`0.115.11`/`0.32.0`) — המתקין מביא אותן ב-apt, ו-`>=` לא מוצמד היה שורש #113. POSIX sh בסוכן, C יחיד (`agent/fanout.c`), vanilla JS בקונסולה |
| טסטים | `python -m pytest tests -q` — חייב לעבור נקי לפני כל commit |
| קצה-לקצה | `python tools/e2e_simulation.py` — שרת אמיתי + ארבע מכונות מדומות במקביל (תהליכונים); רץ גם ב-CI |
| ריפו | github.com/NadavOked/ImageCtl (ציבורי!) · `gh` מחובר · CI ב-GitHub Actions |
| אפיון | `docs/imagectl-spec.md` (v1.1) — מה ולמה; `docs/interfaces.md` — המבנים |
| גרסאות | semver בתגים — הגרסה הנוכחית היא ה-tag האחרון (`git describe --tags`); תג לכל תיקון. **minor רק ליכולת שהמפעיל רואה** — מסך חדש, כפתור חדש, משהו שהוא יכול להצביע עליו. **כל השאר patch**, כולל מודול פנימי חדש בשרת, כלי מעבדה, ו-refactor |
| שרת פיתוח | `python -m server.main --server-url http://127.0.0.1:8080 --data-dir <tmp> --images <tmp>` |

`docs/interfaces.md` הוא מקור האמת למה שעובר בין רכיבים — לא ממציאים
שדות ולא משנים מבנים בלי לעדכן אותו. לכל רכיב יש מסמך הסבר ב-`docs/`.

## עקרונות שאסור לשבור

1. **ברירת המחדל היא תמיד דיסק מקומי.** כל מצב לא ברור — schema זר,
   MAC לא רשום, שרת שקט, סוג task לא מוכר — מסתיים באתחול רגיל.
2. **שורת הפקודה של הקרנל נקייה מפרטי משימה.** רק `imagectl.server`
   ו-`imagectl.mode=recovery`. יש טסטים משני הצדדים.
3. **הדיסק הוא מקור האמת לאימג'ים** (תיקייה + manifest.json); ה-DB רק
   למה שאין לו ייצוג טבעי כקבצים.
4. **אין זריקת בלוק בשקט.** יעד שאיבד בייטים = יעד שנכשל, בגלוי.
5. **פעולה שלא הצליחה לבדוק — נכשלת, לא מוותרת.** "לא הצלחנו לבדוק"
   ו"בדקנו, הכל תקין" הם שני מצבים שונים, ואסור לקפל אותם לאחד. הצלחה
   נקבעת לפי **ראיה חיובית** — קוד תשובה, מונה, ערך שנקרא בחזרה — ולא
   לפי היעדר סימן כישלון. **הדפוס הזה חזר בכל שכבה בפרויקט:**
   `if found:` על ‏`None`/`[]` בבדיקת DHCP קיים (#53 — השומר של
   ההגדרה המסוכנת ביותר, שלא עבד) · `http=000` שנספר כ"הנתיב פתוח" ·
   ‏`-ErrorAction SilentlyContinue` שהופך "אין הרשאה" ל"אין מכונות
   רצות" · ‏`[ -e ] && cp` שדילג על קונברטר gconv חסר (#33, פעמיים) ·
   ‏`pytest` שיוצא 0 כשחבילה שלמה דילגה (#52) · קוד יציאה של `sgdisk`
   שנבלע ב-`sleep 1` (#51). לפני כל `|| true`, ‏`2>/dev/null`,
   ‏`SilentlyContinue` ו-`skipif` — לשאול מה קורה כשהבדיקה **עצמה**
   נכשלת. אם התשובה היא "נראה כמו הצלחה", זה באג.
6. **אימות sha256 לפני שאימג' נכנס לספרייה** (קליטה/העלאה) — פגום
   נתפס שם, לא מול כיתה.
7. **פעולות הרסניות מאחורי הקלדת שם** (מחיקת אימג', עצירת סבב).
8. מגבלת ~300 שורות לקובץ · UI בעברית RTL, מצב בהיר ברירת מחדל ·
   פלט GRUB תמיד ASCII · הסוכן POSIX sh בלבד (busybox ash).

## תרחישי QA קריטיים

- **"מחשב בלי משימה עולה מהדיסק תוך שניות" — בוטל ב-2026-08-30 (#140),
  במודע.** נדב הכריע שתחנת תלמיד ומחשב בנייה יגיעו **ישר** למסך בחירה
  גלוי בין דיסק מקומי ל-ImageCtl, בלי ESC ובלי הגבלת זמן
  (‏`timeout_style=menu`, ‏`timeout=-1`), והמחיר הוצג לו במפורש ואושרר:
  אתחול רגיל של תחנה **ממתין לאדם**, וכיתה של 30 מחשבים היא 30 מסכים
  שממתינים. ‏`set default=local` אינו מגן — ברירת מחדל בלי טיימר אינה
  נבחרת לעולם. **מה שכן נבדק כתרחיש קריטי:**
  - מכונה שאינה מזוהה — MAC לא רשום או schema לא מוכר — עולה מהדיסק
    **מיד** (`timeout=0`), עם ערך אחד בקובץ: בלי תפריט, בלי ImageCtl
    ובלי שחזור (עיקרון 1; "אם אין MAC הוא לא אמור לקבל GRUB"). זה
    שלב 7 בהדגמה ("מחשב ברשת הרגילה של המכללה").
  - שרת כבוי — PXE נכשל בקושחה והתחנה עולה מהדיסק בלי לעבור בתפריט
    שלנו כלל (שלב 8 בהדגמה).
  - מחשב שיכפול (`cloner`) עולה ישר לסוכן, בלי מסך בחירה שימתין לאדם
    ליד מכונה שאין לה מערכת מקומית (#17).
- hello בסבב פתוח מצטרף ומאפס טיימר; done לא מקבל את הסבב שוב
  (אחרת לולאת שחזור).
- משתמש deploy: אימג'ים וסבבים בלבד, כל השאר 403.
- כשל בתחנה/מגירה אחת לא עוצר את השאר.
- המנהל האחרון לא ניתן למחיקה/הורדה.

## Gotchas שנלמדו בדם

- **‏`v0.11.0`, ‏`v0.12.0` ו-`v0.13.0` הן קפיצות minor שרק אחת מהן
  מוצדקת.** ‏`v0.13.0` (מתג SSH) היא יכולת שהמפעיל רואה — נכון.
  ‏`v0.11.0` (`server/bootguard.py`, מודול פנימי) ו-`v0.12.0`
  (‏`tools/lab/demo.ps1`, כלי מעבדה) היו צריכות להיות patch. התגים לא
  שוכתבו — הם דחופים לריפו ציבורי, ומחיקת תג שפורסם גרועה מגרסה
  גבוהה מדי. **הכלל המחייב הוא זה שבטבלה למעלה, לא מה שההיסטוריה
  מראה.** "רכיב חדש" בנוסח הישן היה עמום, וזה מה שהוא הוליד.
- **`from __future__ import annotations` שובר FastAPI** כשהאנוטציה
  (Request) מיובאת בתוך פונקציה — ה-endpoint מחזיר 422. קרה פעמיים.
- **שני תהליכונים על חיבור sqlite אחד** = "cannot commit" אקראי — הם
  חולקים *מצב טרנזאקציה* אחד. ‏`db.Database` נותן חיבור **לכל**
  תהליכון (‏WAL מרשה), ולא רק לתהליכון הרקע: ‏uvicorn מריץ כל `def`
  רגיל — וכל dependency סינכרוני, כולל `current_user` — בתהליכון
  מהמאגר, ולכן כל בקשת קונסולה תוך כדי hello היא שני כותבים (#54).
- **‏`UPDATE` מותנה שלא תאם שורה עדיין מחזיק את נעילת הכתיבה** עד
  ‏commit/rollback. קורא שזרק חריגה בלי `rollback` משאיר נעילה
  יתומה, והשאר מקבלים `database is locked` אחרי `busy_timeout` שלם.
  זה נראה כמו עומס וזה קורא שכבר ויתר — ‏`db.update_one` משחרר
  במקום אחד ולא כשורה שצריך לזכור בכל אתר (#54).
- **עץ מודולים שלא נארז ב-initramfs נכשל בשקט, ובצורה שנראית לא
  קשורה.** בלי `kernel/fs` המכונה עגנה רק NTFS דרך FUSE — ומכאן שם
  המחשב בלינוקס לא נכתב, `used_bytes` היה 0 בכל מניפסט,
  ו-`secure_boot` דיווח `false` על מכונה שהוא דלוק בה (#84).
  ‏`REQUIRED_MODULES` ב-`build_initramfs.sh` מצהיר על פלטפורמות
  היעד **ועוצר את הבנייה** כשמודול מוצהר חסר, כי אחרת זה מתגלה רק
  מול מכונה אמיתית (#78).
- **מטמון דפדפן לקבצי הקונסולה**: כל שינוי סטטי מחייב bump ל-`?v=`
  ב-index.html, אחרת JS ישן רץ מול API חדש.
- **`prompt()/confirm()` חסומים** בדפדפנים מוטמעים — רק המודאל של
  הקונסולה (`sheet()` ב-console.js).
- **SVG עם viewBox בלבד קורס ל-0** תחת max-height — לתת height מפורש.
- `tests/test_fanout.py` דורש gcc — מדולג בווינדוס, רץ ב-CI ועל לינוקס.
  ה-sim של סוכנים נעשה ב-HTTP מול השרת, לא בהרצת הסקריפטים.
- `fanout` נכשל בשקט אם המאגר קטן מ-READ_CHUNK (1MB) — exit 2 לפני
  בייט ראשון. כונן איטי-אך-יציב לא "עומד בקצב": הוא מעכב את הזרם,
  והמדד הוא כמה זמן הזרם מחכה בגללו (ROOM_GRACE_MS), לא אם הבלוק הבא נכנס.
- **DHCP מהקונסולה כותב ל-`/etc/dnsmasq.d/imagectl-dhcp.conf`** ומריץ
  `systemctl restart dnsmasq` — דורש root ו-`ReadWritePaths` ביחידה.
  בבדיקות מזריקים `dhcp_hooks` ל-`create_app`; לעולם לא נוגעים במכונה.
- הודעות commit ארוכות דרך `git commit -F <קובץ>` — here-string של
  PowerShell נשבר על תוכן מסוים.
- **תהליך-בן עם `stdout=PIPE` שאיש לא קורא נחסם** כשהחוצץ מתמלא
  (~64KB) — שרת הסימולציה "הפסיק לענות" אחרי כמה מאות שורות לוג.
  פלט של תהליך ארוך הולך לקובץ, לא ל-PIPE.
- **תהליך-בן בטסטים חייב `stdin=subprocess.DEVNULL` בווינדוס** — בריצה
  רב-קבצית של pytest ה-handle של stdin נשבר תחת capture, וכל subprocess
  שיורש אותו נופל ב-`WinError 50` ב-DuplicateHandle. קובץ בודד עובר,
  הריצה המלאה נופלת — ולכן זה נראה כמו flakiness סביבתי (#14).
- **libhivex צריך את מודולי ה-gconv של glibc** — הוא ממיר שמות מפתחות
  עם iconv, וב-initramfs בלי `gconv/` כל חיפוש מפתח נכשל "key not found"
  בשקט. ככה שם המחשב לא נכתב חודשים (#33). ‏hivexget הוא בכלל wrapper
  של shell ל-hivexsh — לא כלי עצמאי.
- **לולאת polling צפופה מדי בווינדוס ממצה פורטים** (TIME_WAIT):
  חיבור חדש לכל בקשה מתעכב שניות ומשבש טסטים תלויי-טיימר. השהיה
  של ≥0.1s בין דגימות בסימולציה.
- **`capture_output` ממתין לסגירת הצינור, לא ליציאת התהליך.** בטסט
  שבודק פקיעת המתנה, כל תהליך יתום (`partclone`/`zstd` מתחת לתת-מעטפת
  שנהרגה) מחזיק עותק של הצינור — והבדיקה "נתקעת" דקה בזמן שהסוכן
  דיווח ויצא תוך 8 שניות. הכשל נראה כמו באג בקוד שנבדק, והוא בכלי
  הבדיקה. פלט לקובץ ו-`DEVNULL` לצינורות.
- **ב-POSIX sh אין דרך נקייה להרוג קבוצת תהליכים** (אין job control):
  ‏`kill` על תת-מעטפת הורג את המנהלת ומשאיר את `partclone`/`zstd`
  מתחתיה חיים וחסומים. לכן תקרת המתנה לבדה לא מספיקה — הטיפול בשורש
  הוא תקרה **במקור הזרם עצמו** (דגלי `udp-receiver`, ‏`--speed-limit`
  של curl) ושחרור הקורא בפתיחת ה-fifo לכתיבה (`unblock_fifo`).

## מה נשאר פתוח (נכון ל-2026-08-26)

נספח ג' באפיון (`docs/imagectl-spec.md`) סגור ב-v0.2.0. נשאר לחומרה:
שלב א' של תוכנית הבדיקות (שני מחשבים על כבל — shim/GRUB/דרייברים/udpcast),
`test_fanout.py` מול SSD אמיתי, ושחזור לינוקס (ext4/btrfs, `/etc/hostname`)
על מכונה אמיתית — נכתב לפי המניפסט, עוד לא הורץ מול דיסק.
