# ImageCtl -- the DCUI lives on tty1 (#1195). Kernel warnings (nftables
# "imagectl-drop" log lines, driver chatter) went to the console and drew
# over the screen; they stay in the journal (journalctl -k), the console
# only gets crit and worse.
kernel.printk = 3 4 1 3
